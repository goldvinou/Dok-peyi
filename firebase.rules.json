{
  "rules": {
    ".read": false,
    ".write": false,

    "demandes": {
      // Clients can create orders; they cannot read or modify existing ones
      "$orderId": {
        ".read": false,
        ".write": "!data.exists()",
        ".validate": "newData.hasChildren(['service','prenom','email','statut','createdAt'])"
      }
    },

    "statusUpdates": {
      "$orderId": {
        ".read": false,
        ".write": false
      }
    },

    "processed-events": {
      // Server-side only — idempotence log for Stripe webhook events
      "$eventId": {
        ".read": false,
        ".write": false
      }
    }
  }
}
